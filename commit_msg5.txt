feat: sales domain agent through the action gate

- agent/sales_agent.py: watch draft quotations, plan confirmations,
  submit through the gate; never confirms on its own
- sales_demo.py: scan / pending / full-loop modes (create -> approve ->
  confirm -> verify -> cancel -> cleanup); only touches demo-marked
  records, never real business orders
- gate.py: call method now passes ids via execute_kw (avoids odoorpc
  Model.browse, which breaks on Odoo 19)
- Fix OCA tier_validation for Odoo 19: models.NewId removed ->
  isinstance(rec.id, int); res.users.groups_id -> group_ids
- README: sales agent section, Odoo 19 adaptation table, roadmap update
